# Node
node_modules/

# Secrets — never upload .env or .env.production
.env
.env.*
!.env.example

# Expo build outputs
.expo/
dist/
web-build/

# Native dirs (generated by prebuild — not needed for Vercel web)
ios/
android/
apps/mobile/ios/
apps/mobile/android/

# Local
*.log
.DS_Store
*.tsbuildinfo

# Docs/other apps (keep Vercel upload lean — only the web app is served)
# (uncomment if Vercel upload becomes large)
# docs/
